% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piggyback.R
\name{load_predictions}
\alias{load_predictions}
\title{Load match predictions}
\usage{
load_predictions(source = c("remote", "local"), filter_future = FALSE)
}
\arguments{
\item{source}{Data source: "local" loads from pannadata/predictions/ (errors
if not found), "remote" always downloads latest from GitHub release first.}

\item{filter_future}{If TRUE, returns only matches with match_date >= today.}
}
\value{
Data frame of match predictions.
}
\description{
Loads match predictions from local cache or downloads from GitHub Releases.
}
\examples{
\dontrun{
# Load from remote (always downloads latest)
preds <- load_predictions(source = "remote")

# Load only future matches
preds <- load_predictions(source = "remote", filter_future = TRUE)

# Load from local cache
preds <- load_predictions(source = "local")
}
}
