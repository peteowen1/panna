% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{fit_spm_panel}
\alias{fit_spm_panel}
\title{Fit a role-pooled elastic-net SPM model on the panel, predicting the
windowed prior-free RAPM target}
\usage{
fit_spm_panel(
  panel,
  target = c("offense", "defense", "net"),
  role_pooling = TRUE,
  role_ambivalent_cols = .spm_panel_role_ambivalent_cols(),
  deviation_penalty_mult = 5,
  alpha = 0.5,
  weight_transform = c("sqrt", "linear"),
  min_window_minutes = 0,
  eiv_rescale = FALSE,
  eiv_m0 = 8000,
  eiv_floor = 0.4,
  sign_constraints = TRUE,
  predictor_cols = NULL,
  nfolds = 5,
  seed = NULL
)
}
\arguments{
\item{panel}{Output of \code{build_spm_panel()} (or any data.table/data.frame
carrying the same \code{target_provenance} attribute + columns).}

\item{target}{One of \code{"offense"}, \code{"defense"}, \code{"net"} -- which target
column (\code{offense_target}/\code{defense_target}/\code{rapm_target}) to fit.}

\item{role_pooling}{Add role-group deviation columns (default \code{TRUE}).
\code{FALSE} = global-only (candidate S1).}

\item{role_ambivalent_cols}{Feature columns to build deviations for
(default \code{.spm_panel_role_ambivalent_cols()}).}

\item{deviation_penalty_mult}{Multiplier applied to deviation columns'
\code{penalty.factor} relative to global columns (default 5 -- higher
shrinkage, sec 3.1's partial-pooling design).}

\item{alpha}{Elastic-net mixing (default 0.5).}

\item{weight_transform}{\code{"sqrt"} (default) or \code{"linear"} (sec 2.3.1 --
the plan restricts this study to these two; \code{fit_spm_model()}'s
\code{"log"}/\code{"none"} are out of scope here).}

\item{min_window_minutes}{Drop panel rows below this window-minutes floor
before fitting (default 0 = no floor; sec 2.3.1's blunt alternative to
weighting).}

\item{eiv_rescale}{Apply the errors-in-variables target rescale \code{y / max(r_hat, eiv_floor)}, \code{r_hat = window_minutes / (window_minutes + eiv_m0)} (default \code{FALSE}; sec 2.3.2).}

\item{eiv_m0}{Implied prior minutes at 0 the ridge penalty is equivalent
to (default 8000 -- the Wave-1 attenuation study's empirical estimate,
\verb{data-raw/spm-redesign/03_attenuation_diagnostics.R} /
\code{attenuation_band_summary.csv}; only used when \code{eiv_rescale = TRUE}).}

\item{eiv_floor}{Minimum \code{r_hat} before rescaling (default 0.4, per sec
2.3.2).}

\item{sign_constraints}{Apply sec 3.1's sign constraints for the chosen
\code{target} (default \code{TRUE}; no-op for \code{target = "net"}, which has no
hand-curated list in 05_spm.R either).}

\item{predictor_cols}{Global predictor columns (default \code{NULL} = the
canonical \code{.spm_opta_predictor_cols(panel)} selector, guaranteeing
feature-set parity with the existing career-level SPM).}

\item{nfolds}{CV folds (default 5).}

\item{seed}{RNG seed for the grouped fold assignment (default \code{NULL}).}
}
\value{
A \code{cv.glmnet} object with \code{panna_metadata} (type \code{"spm_panel"},
\code{target}, \code{predictor_cols} (global), \code{dev_names}, \code{role_groups},
config echo, \code{feature_sds}, \code{n_observations}, \code{lambda_min}) --
\code{predict_spm_panel()} scores new panel rows against it.
}
\description{
The Wave-2 estimator (BOX-SCORE-VALUE-SPM-REDESIGN.md sec 2.2/2.3/3.1):
plain \code{glmnet::cv.glmnet()} with (a) player-grouped CV folds (never
random rows -- R5), (b) an optional role-group x feature interaction
block for the restricted role-ambivalent feature set, shrunk toward 0 via
a higher \code{penalty.factor} (partial pooling -- no new machinery beyond
glmnet), (c) minutes weighting (\code{sqrt}/\code{linear}), (d) an optional
errors-in-variables target rescale, (e) sign constraints extending
05_spm.R's defense lists with sec 3.1's new offense set.
}
\details{
Circularity guard: re-checks the panel's \code{target_provenance} attribute
via \code{assert_prior_free_target()} (the SAME function \code{build_spm_panel()}
calls on the raw target) before fitting -- a training entry point that
trusts an un-stamped panel is exactly the gap sec 2.4.1 closes.
}
\seealso{
Other spm panel: 
\code{\link{assert_asof_panel_window}()},
\code{\link{assert_grouped_player_folds}()},
\code{\link{build_spm_panel}()},
\code{\link{classify_role_group}()},
\code{\link{fit_spm_panel_xgb}()},
\code{\link{make_grouped_player_foldid}()},
\code{\link{predict_spm_panel}()},
\code{\link{predict_spm_panel_net}()},
\code{\link{predict_spm_panel_xgb}()}
}
\concept{spm panel}
