% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{.ng_defensive_acts}
\alias{.ng_defensive_acts}
\title{Count each player's defensive acts in a match}
\usage{
.ng_defensive_acts(actions, weight_by_value = FALSE)
}
\arguments{
\item{actions}{SPADL actions with \code{match_id}, \code{player_id}, \code{action_type},
\code{result}, and \code{epv_delta} when \code{weight_by_value} is \code{TRUE}.}

\item{weight_by_value}{Weight each act by how much its own \code{epv_delta} moved
the game rather than counting it. Fixes "a routine clearance weighs the
same as a goal-line block" without reaching for the player's own payments,
which is what made \code{.ng_defensive_value()} run away.}
}
\value{
\code{match_id}, \code{player_id}, \code{dacts}.
}
\description{
The weight behind \code{ng_spread_pools(dacts_share = )}. Successful tackles,
interceptions, clearances, recoveries, aerials and keeper actions -- the
things a player visibly did to win or deny the ball.
}
\details{
Two caveats worth stating rather than discovering later. It counts the
\strong{whole match}, so a pool generated in the 10th minute is routed partly by
acts made in the 80th; that is acceptable for a descriptive metric and would
not be for a predictive one. And it counts acts, not their value, so a
routine clearance weighs the same as a goal-line block.
}
\keyword{internal}
