% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{.spm_panel_window_features}
\alias{.spm_panel_window_features}
\title{Build one (player, vintage year) window of raw counting-stat totals}
\usage{
.spm_panel_window_features(
  match_stats,
  cutoff_year,
  min_year,
  leagues = NULL,
  include_xmetrics = TRUE,
  xmetrics_source = "local"
)
}
\arguments{
\item{match_stats}{data.table, \code{compute_match_level_opta_stats()} shape
(needs \code{player_id}, \code{match_id}, \code{season_end_year}, \code{position},
\code{position_side}, \code{total_minutes}, \code{league}, plus the raw counting
columns).}

\item{cutoff_year}{Integer; rows from seasons \verb{< cutoff_year} are kept.}

\item{min_year}{Integer; rows from seasons \verb{< min_year} are dropped
(window is \verb{[min_year, cutoff_year)}).}

\item{leagues}{Optional character vector to restrict to (\code{NULL} = all).}

\item{include_xmetrics}{Whether to attempt xMetrics enrichment (best-effort
-- see \code{build_spm_panel()}).}

\item{xmetrics_source}{\code{"local"} or \code{"remote"}, passed to
\code{enrich_match_stats_with_xmetrics()}.}
}
\value{
data.frame, one row per player, feature columns + \code{role},
\code{role_group}, \code{window_minutes}, \code{n_matches}. \code{NULL} if no rows survive
the window/league filter. Attribute \code{xmetrics_included} (logical).
}
\description{
Sums the raw Opta counting columns (\code{.get_opta_col_mapping()} panna
names, already present on \code{match_stats} -- it is
\code{compute_match_level_opta_stats()} output) over matches with
\code{season_end_year} in \verb{[min_year, cutoff_year)}, derives a minutes-weighted
modal \code{classify_role()}/\code{classify_role_group()} per player, then runs the
SAME per-90 + derived-feature pipeline \code{aggregate_opta_stats()} uses
(\code{.calculate_opta_per90()}, \code{.calculate_opta_derived_features()}) so the
window-level feature columns are byte-identical in name/construction to
the career-level SPM's -- the column contract \code{.spm_opta_predictor_cols()}
selects against.
}
\keyword{internal}
