% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ng_breakdown.R
\name{.ng_breakdown}
\alias{.ng_breakdown}
\title{Split each published player-match's net goals by play type}
\usage{
.ng_breakdown(pay, pre_fold, published, tol = 1e-09)
}
\arguments{
\item{pay}{Payment table after \code{\link[=ng_spread_pools]{ng_spread_pools()}}.}

\item{pre_fold}{\code{\link[=ng_player_game]{ng_player_game()}} output (before folding), with \code{net_goals}.}

\item{published}{The published frame after \code{\link[=ng_reconcile_margin]{ng_reconcile_margin()}}: one row
per player-match with \code{net_goals} and \code{ng_recon}.}

\item{tol}{Largest allowed gap between a row's categories and its published
\code{net_goals}. Rounding level: anything bigger is a dropped or doubled payment.}
}
\value{
data.table: \code{match_id}, \code{player_id}, \code{category}, \code{value} (goals,
positive is good for the player), for every published row with net goals.
}
\description{
A published \code{net_goals} is built in three steps, and the breakdown keeps each
visible so the parts add up exactly:
\enumerate{
\item the ledger's payments to the player (\code{\link[=ng_player_game]{ng_player_game()}} sums them), split
here by play type, with the team pool share as its own category;
\item \code{\link[=ng_fold_unpublished]{ng_fold_unpublished()}}, which hands the value of players the published
frame does not carry (unused or late substitutes) to their team-mates by
minutes: "Share of unlisted team-mates";
\item \code{\link[=ng_reconcile_margin]{ng_reconcile_margin()}}'s anchor to the real goal difference (\code{ng_recon}).
}
}
\keyword{internal}
