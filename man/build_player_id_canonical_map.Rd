% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_id_canonical.R
\name{build_player_id_canonical_map}
\alias{build_player_id_canonical_map}
\title{Build a player_id -> canonical_id lookup}
\usage{
build_player_id_canonical_map(
  lineups,
  min_dominance_ratio = 0.05,
  require_team_overlap = TRUE
)
}
\arguments{
\item{lineups}{Data.table of lineups (full or filtered).}

\item{min_dominance_ratio}{Numeric. Alt must have at most this fraction
of the main's appearance count to be a merge candidate. Default 0.05
(alt <= 5\% of main). Catches data-entry errors (alt has 2-10 matches
vs main has hundreds) without merging mid-tier namesakes.}

\item{require_team_overlap}{Logical. Require at least one common team
between alt and main. Default TRUE. Catches the "two Danilos played
for different national teams" namesake case.}
}
\value{
Data.table with columns \code{player_id} (every observed id) and
\code{canonical_id} (its main mapping; equals \code{player_id} for non-merged
dominant ids).
}
\description{
Build a player_id -> canonical_id lookup
}
\keyword{internal}
