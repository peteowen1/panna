% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{build_spm_panel}
\alias{build_spm_panel}
\title{Build the SPM training panel: one row per (player, vintage year)}
\usage{
build_spm_panel(
  match_stats,
  rapm_window_targets,
  vintage_years = 2019:2026,
  window_years = 5L,
  leagues = NULL,
  include_xmetrics = TRUE,
  xmetrics_source = c("local", "remote"),
  include_gk = FALSE,
  strict_window_check = TRUE
)
}
\arguments{
\item{match_stats}{data.table, \code{compute_match_level_opta_stats()} shape
(e.g. \verb{cache-skills/01_match_stats.rds}). Must have \code{season_end_year}
OR \code{season} (the latter is converted via \code{extract_season_end_year()}).}

\item{rapm_window_targets}{The list from \code{rapm_window_targets.rds}
(04b_rapm_window_targets.R), keyed by vintage year.}

\item{vintage_years}{Integer vector of vintage years to build (default
\code{2019:2026}, matching Wave 1).}

\item{window_years}{Window length (default 5, matching sec 2.1). Must
match the window \code{rapm_window_targets} was built with, or
\code{strict_window_check} aborts.}

\item{leagues}{Optional character vector to restrict to (\code{NULL} = all --
full build; pass a small set for a smoke-scale build).}

\item{include_xmetrics}{Attempt best-effort xMetrics enrichment per
vintage (default \code{TRUE}). Degrades gracefully (box-only, \code{cli_warn}) if
local/remote xMetrics coverage is unavailable for a vintage's window --
see \code{.spm_panel_window_features()}.}

\item{xmetrics_source}{\code{"local"} or \code{"remote"}.}

\item{include_gk}{Include GK rows in the returned panel (default \code{FALSE}
-- see above). When \code{FALSE}, dropped-row counts are reported via
\code{cli::cli_inform()}.}

\item{strict_window_check}{Abort if a vintage's actual window (min_year,
cutoff_year) doesn't match the corresponding \code{rapm_window_targets}
entry's \code{window} (default \code{TRUE} -- window misalignment defeats the
whole point of the panel design; sec 2.2).}
}
\value{
data.table, one row per (player_id, vintage_year), with feature
columns, \code{role}, \code{role_group}, \code{window_minutes}, \code{n_matches},
\code{offense_target}, \code{defense_target}, \code{rapm_target} (net), \code{vintage_year},
\code{window_min_year}, \code{window_max_year}. Attributes: \code{target_provenance}
(\code{"prior_free_rapm_window"}), \code{builder_params} (list of the arguments
above + \code{built_at}).
}
\description{
BOX-SCORE-VALUE-SPM-REDESIGN.md sec 2.2/3.1: for each vintage \code{Y} in
\code{vintage_years}, features are the player's box+xMetrics per-90 aggregates
over \verb{[Y - window_years, Y)} (\code{.spm_panel_window_features()}), target is
the SAME-window prior-free RAPM for \code{Y} (\code{rapm_window_targets.rds},
04b_rapm_window_targets.R). Feature/target window alignment is checked,
not assumed (\code{strict_window_check}).
}
\details{
Circularity guard: calls \code{assert_prior_free_target(rapm_window_targets)}
before touching any rating, and stamps the returned panel's
\code{target_provenance} attribute so \code{fit_spm_panel()} can re-verify it
without needing the raw target object again.

GK rows are excluded by default (\code{include_gk = FALSE}) -- panna#159's
keeper-rows-only design is the intended home for GK \code{spm_value}/SPM
pricing (BOX-SCORE-VALUE-SPM-REDESIGN.md sec 3.1, R7); this panel is
outfield-only until that lands.
}
\seealso{
Other spm panel: 
\code{\link{assert_asof_panel_window}()},
\code{\link{assert_grouped_player_folds}()},
\code{\link{classify_role_group}()},
\code{\link{fit_spm_panel}()},
\code{\link{fit_spm_panel_xgb}()},
\code{\link{make_grouped_player_foldid}()},
\code{\link{predict_spm_panel}()},
\code{\link{predict_spm_panel_net}()},
\code{\link{predict_spm_panel_xgb}()}
}
\concept{spm panel}
