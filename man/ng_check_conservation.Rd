% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_check_conservation}
\alias{ng_check_conservation}
\title{Check the ledger against the scoreline}
\usage{
ng_check_conservation(pay, fixtures, verbose = TRUE)
}
\arguments{
\item{pay}{Output of \code{ng_build_ledger()}}

\item{fixtures}{Fixtures with \code{match_id}, \code{home_score}, \code{away_score}}

\item{verbose}{Print the summary. Default \code{TRUE}.}
}
\value{
A data.table, one row per match: \code{ledger}, \code{gd}, \code{err}.
}
\description{
The identity is \code{sum(value_home) == home_score - away_score} per match. It is
\strong{approximate} in football, unlike torp's: panna's EPV asks "who scores next
\emph{this half}" and the shot override swaps model EPV for xG, so the telescoping
terms nearly but not quite cancel. Measured on ENG 2024-2025 the median error
is 0.198 goals and the worst match 1.339.
}
\details{
This function is deliberately a \emph{report}, not a gate: it prints what the gap
is rather than forcing it to zero. Anything that forces the total would make
every downstream assertion vacuous.
}
\seealso{
Other net_goals: 
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_player_game}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
