% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{.ng_credit_terms}
\alias{.ng_credit_terms}
\title{Split each action's value into credit and blame}
\usage{
.ng_credit_terms(dt, shares)
}
\arguments{
\item{dt}{Actions with \code{value_home}, \code{action_type}, \code{result}, recipient
columns, and optionally \code{true_possession_change} / \code{true_next_player} from
\code{ng_build_adjacency()} and \code{xpass} from \code{add_xpass_to_spadl()}.}

\item{shares}{Output of \code{ng_shares()}.}
}
\value{
A long data.table of payments with \code{role} and \code{play_type} tags.
}
\description{
The \strong{margin convention}. Subdivides every row's \code{value_home} among
recipients, allocating it ONCE and splitting it across both sides.
\code{.ng_credit_terms_team()} is the other convention, which books it twice.
Subdivides every row's \code{value_home} among recipients. The terms for a row
always sum to that row's \code{value_home}, so no rule can change a match total --
only who is paid.
}
\details{
Panna's row shape differs from torp's in one way worth stating, because it
changes where the decision term lives. \code{calculate_action_epv()} overrides a
shot's EPV with its xG and then recalculates the preceding row's delta to
target that xG. So the value of working the ball into a shot worth 0.30
instead of 0.05 -- the decision term, averaging +0.0716 goals a shot -- is
already paid on the pass before it. The shot row carries only \code{1 - xG} on a
goal or \code{-xG} on a miss, which is the surprise. This function therefore
splits surprises; it does not reconstruct a decision term that is already
allocated elsewhere.
}
\keyword{internal}
