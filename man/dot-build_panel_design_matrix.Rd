% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{.build_panel_design_matrix}
\alias{.build_panel_design_matrix}
\title{Build the panel design matrix: global feature columns + optional
role-group x feature deviation columns}
\usage{
.build_panel_design_matrix(
  panel,
  global_cols,
  deviation_cols = character(0),
  role_groups = character(0)
)
}
\arguments{
\item{panel}{data.table/data.frame with \code{global_cols} and (if
\code{deviation_cols} non-empty) a \code{role_group} column.}

\item{global_cols}{Character vector of predictor column names.}

\item{deviation_cols}{Character vector (subset of \code{global_cols}) to
generate role-group deviation columns for. \code{character(0)} = no pooling.}

\item{role_groups}{Character vector of role-group codes to build
deviations for (typically \code{.spm_panel_outfield_role_groups()}).}
}
\value{
List: \code{X} (numeric matrix, columns = \code{global_cols} then all
\verb{dev__*} columns in group-major order), \code{dev_names} (character vector,
possibly empty).
}
\description{
Deviation column naming: \verb{dev__<role_group>__<feature>} (role_group codes
from \code{classify_role_group()} are alphanumeric-only, so this is a safe,
unambiguous split point -- \code{.panel_base_feature_of()} reverses it).
}
\keyword{internal}
