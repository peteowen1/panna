% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xg_model.R
\name{fit_xg_model}
\alias{fit_xg_model}
\title{Fit xG Model using XGBoost}
\usage{
fit_xg_model(
  shot_features,
  exclude_penalties = TRUE,
  season_feature = FALSE,
  nfolds = 5,
  max_depth = 6,
  eta = 0.05,
  subsample = 0.8,
  colsample_bytree = 0.8,
  nrounds = 500,
  early_stopping_rounds = 50,
  verbose = 1
)
}
\arguments{
\item{shot_features}{Data frame from prepare_shots_for_xg()}

\item{exclude_penalties}{Whether to exclude penalties from training (default TRUE).
Every penalty is taken from the same spot, so the geometry features carry no
information on them; they are scored at \code{\link{PENALTY_XG}} instead.}

\item{season_feature}{Add a \code{season_num} term (default FALSE). See the
comment at the feature list for the evidence and the extrapolation caveat.}

\item{nfolds}{Number of CV folds (default 5)}

\item{max_depth}{Maximum tree depth (default 6)}

\item{eta}{Learning rate (default 0.05)}

\item{subsample}{Row subsampling (default 0.8)}

\item{colsample_bytree}{Column subsampling (default 0.8)}

\item{nrounds}{Maximum boosting rounds (default 500)}

\item{early_stopping_rounds}{Early stopping patience (default 50)}

\item{verbose}{Print progress (0=silent, 1=progress)}
}
\value{
List with:
\itemize{
\item model: Fitted XGBoost model
\item cv_result: Cross-validation results
\item importance: Feature importance
\item calibration: Calibration metrics
\item panna_metadata: Model metadata
}
}
\description{
Trains an XGBoost classifier to predict goal probability from shot features.
Uses cross-validation to find optimal number of boosting rounds.
}
\examples{
\dontrun{
shots <- load_opta_shot_events("ENG", "2024-2025")
shot_features <- prepare_shots_for_xg(shots)
xg_model <- fit_xg_model(shot_features)
}
}
\seealso{
Other epv: 
\code{\link{aggregate_player_xmetrics}()},
\code{\link{assign_epv_credit}()},
\code{\link{convert_opta_to_spadl}()},
\code{\link{enrich_match_stats_with_xmetrics}()},
\code{\link{fit_epv_model}()},
\code{\link{fit_xgot_model}()},
\code{\link{fit_xpass_model}()},
\code{\link{get_or_build_spadl}()},
\code{\link{load_epv_model}()},
\code{\link{load_xg_model}()},
\code{\link{load_xgot_model}()},
\code{\link{load_xpass_model}()},
\code{\link{pb_download_epv_models}()},
\code{\link{predict_xg}()},
\code{\link{predict_xgot}()},
\code{\link{predict_xpass}()},
\code{\link{save_epv_model}()}
}
\concept{epv}
