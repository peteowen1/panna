% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psr.R
\name{.psr_checkpoint_reject_reason}
\alias{.psr_checkpoint_reject_reason}
\title{Why a checkpoint was rejected (empty string = usable)}
\usage{
.psr_checkpoint_reject_reason(cp, fingerprint, n_dates)
}
\arguments{
\item{cp}{The deserialized checkpoint (or \code{NULL} if it was missing or
unreadable).}

\item{fingerprint}{The fingerprint computed for the CURRENT call.}

\item{n_dates}{Number of ref_dates in the current call, used to reject a
checkpoint whose recorded position can't apply to this run.}
}
\value{
Empty string if usable, else a short human-readable reason.
}
\description{
Split from \code{.psr_checkpoint_usable()} so a resume failure logs the
ACTUAL reason. The message used to say "fingerprint mismatch"
unconditionally, which is misleading when the real cause was a truncated
checkpoint or an out-of-range position -- exactly the situation where
someone reading the log is trying to work out why a long run restarted
from scratch.
}
\keyword{internal}
