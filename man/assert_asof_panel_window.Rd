% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{assert_asof_panel_window}
\alias{assert_asof_panel_window}
\title{Assert that an as-of training panel for eval vintage \code{Y} contains no rows
from a LATER vintage}
\usage{
assert_asof_panel_window(train_panel, Y)
}
\arguments{
\item{train_panel}{A panel (or panel subset) about to be passed to
\code{fit_spm_panel()}/\code{run_candidate()} for an as-of fit targeting eval
vintage \code{Y}.}

\item{Y}{The eval vintage the fit is being restricted to (features/target
for \code{Y} must come from \code{vintage_year <= Y} only).}
}
\value{
Invisibly \code{TRUE} if no row exceeds \code{Y}; aborts otherwise.
}
\description{
The structural leak guard for the held-out next-window eval gate (sec
5.2): a candidate being scored on vintage \code{Y}'s rows against the \code{Y+1}
target must be fit ONLY on \code{vintage_year <= Y} rows. Fitting on the WHOLE
panel (all vintages pooled) and then scoring vintage \code{Y} against \code{Y+1}'s
target is the exact hindsight leak \code{eval_nextseason.R}'s header documents
as banned for its own pooled-vs-per-season candidate distinction: the
pooled fit would have TRAINED on vintage \code{Y+1}'s own panel row (whose
label literally IS the \code{Y+1} target being scored against), and vintage
\code{Y}'s window overlaps vintage \code{Y+1}'s window in 4 of 5 seasons, so the
pooled model's coefficients partially encode information that shouldn't
be visible yet. \verb{data-raw/spm-redesign/05c_candidates.R}'s
\code{run_candidate_asof()} calls this on its own \code{vintage_year <= Y} subset
before fitting -- this is the assertion made executable, not just a
comment.
}
\seealso{
Other spm panel: 
\code{\link{assert_grouped_player_folds}()},
\code{\link{build_spm_panel}()},
\code{\link{classify_role_group}()},
\code{\link{fit_spm_panel}()},
\code{\link{fit_spm_panel_xgb}()},
\code{\link{make_grouped_player_foldid}()},
\code{\link{predict_spm_panel}()},
\code{\link{predict_spm_panel_net}()},
\code{\link{predict_spm_panel_xgb}()}
}
\concept{spm panel}
