% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_prediction.R
\name{.fit_xgb_model}
\alias{.fit_xgb_model}
\title{Fit XGBoost Model with Cross-Validation}
\usage{
.fit_xgb_model(
  X,
  y,
  params,
  nfolds = 5L,
  nrounds = 500L,
  early_stopping = 30L,
  verbose = 1L,
  group_ids = NULL
)
}
\arguments{
\item{X}{Feature matrix}

\item{y}{Target vector (goal counts for Poisson, integer labels for multinomial)}

\item{params}{XGBoost parameters list (objective, eval_metric, etc.)}

\item{nfolds}{Number of CV folds (default 5)}

\item{nrounds}{Max boosting rounds (default 500)}

\item{early_stopping}{Patience for early stopping (default 30)}

\item{verbose}{Print progress (default 1)}

\item{group_ids}{Optional vector, one entry per row of \code{X}/\code{y}
(e.g. \code{match_id}). When supplied, CV folds are assigned by group
via \code{\link{.grouped_cv_folds}} instead of xgboost's default random
per-row assignment, so a match and its mirrored twin never split across
folds. \code{NULL} (default) keeps the prior per-row random behaviour.}
}
\value{
List with model, cv_result, best_nrounds, metadata
}
\description{
Shared helper for training XGBoost models with k-fold cross-validation
and early stopping. Used by \code{\link{fit_goals_xgb}} (Poisson) and
\code{\link{fit_outcome_xgb}} (multinomial).
}
\keyword{internal}
