% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{make_grouped_player_foldid}
\alias{make_grouped_player_foldid}
\title{Assign player-grouped CV fold ids: every row for a given \code{player_id}
lands in the same fold}
\usage{
make_grouped_player_foldid(player_id, nfolds = 5, seed = NULL)
}
\arguments{
\item{player_id}{Character vector, one entry per panel row. Coerced via
\code{as.character()}; aborts on any \code{NA} (an \code{NA} player_id can't be
consistently grouped -- every occurrence would collide into the same
"fold", silently defeating the R5 guarantee this function exists for).}

\item{nfolds}{Number of folds.}

\item{seed}{Optional RNG seed.}
}
\value{
Integer vector (1..nfolds), same length as \code{player_id}.
}
\description{
BOX-SCORE-VALUE-SPM-REDESIGN.md sec 2.2/R5: overlapping vintage windows
make rows for the same player heavily dependent (a player's 2023 and 2024
panel rows share 4 of 5 window seasons); random row-level CV folds leak
across folds and inflate CV-selected lambda's apparent fit. Folds are
assigned by shuffling UNIQUE player ids into \code{nfolds} buckets
(\code{rep_len()} + \code{sample()}, balanced), then broadcasting per row.
}
\seealso{
Other spm panel: 
\code{\link{assert_asof_panel_window}()},
\code{\link{assert_grouped_player_folds}()},
\code{\link{build_spm_panel}()},
\code{\link{classify_role_group}()},
\code{\link{fit_spm_panel}()},
\code{\link{fit_spm_panel_xgb}()},
\code{\link{predict_spm_panel}()},
\code{\link{predict_spm_panel_net}()},
\code{\link{predict_spm_panel_xgb}()}
}
\concept{spm panel}
