% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psr.R
\name{.psr_checkpoint_usable}
\alias{.psr_checkpoint_usable}
\title{Decide whether a loaded checkpoint may be resumed from}
\usage{
.psr_checkpoint_usable(cp, fingerprint, n_dates)
}
\arguments{
\item{cp}{The deserialized checkpoint (or \code{NULL} if it was missing or
unreadable).}

\item{fingerprint}{The fingerprint computed for the CURRENT call.}

\item{n_dates}{Number of ref_dates in the current call, used to reject a
checkpoint whose recorded position can't apply to this run.}
}
\value{
\code{TRUE} only if the checkpoint is structurally complete and
its fingerprint matches exactly.
}
\description{
Split out of \code{.estimate_prematch_skills_batch()} so the decision that
gates resuming can be tested directly -- accepting a checkpoint built from
DIFFERENT inputs silently splices two computations together (running sums
from one set of inputs, later dates from another) with no error and no
visible symptom, so this is the single most consequential branch in the
streaming/resume path.
}
\keyword{internal}
