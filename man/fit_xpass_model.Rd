% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xpass_model.R
\name{fit_xpass_model}
\alias{fit_xpass_model}
\title{Fit xPass Model using XGBoost}
\usage{
fit_xpass_model(
  pass_features,
  nfolds = 5,
  max_depth = 6,
  eta = 0.1,
  subsample = 0.8,
  colsample_bytree = 0.8,
  nrounds = 500,
  early_stopping_rounds = 50,
  verbose = 1
)
}
\arguments{
\item{pass_features}{Data frame from prepare_passes_for_xpass()}

\item{nfolds}{Number of CV folds (default 5)}

\item{max_depth}{Maximum tree depth (default 6)}

\item{eta}{Learning rate (default 0.05)}

\item{subsample}{Row subsampling (default 0.8)}

\item{colsample_bytree}{Column subsampling (default 0.8)}

\item{nrounds}{Maximum boosting rounds (default 500)}

\item{early_stopping_rounds}{Early stopping patience (default 50)}

\item{verbose}{Print progress (0=silent, 1=progress)}
}
\value{
List with fitted model and metadata
}
\description{
Trains an XGBoost classifier to predict pass completion probability.
}
\examples{
\dontrun{
passes <- prepare_passes_for_xpass(spadl)
xpass_model <- fit_xpass_model(passes)
}
}
