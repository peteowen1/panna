% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_opta.R
\name{.skill_spm_offense_cols}
\alias{.skill_spm_offense_cols}
\title{Canonical offense predictor columns for the SKILL-based SPM}
\usage{
.skill_spm_offense_cols(data)
}
\arguments{
\item{data}{Data frame of candidate features (e.g. \code{spm_train_data})}
}
\value{
Character vector of offense predictor columns present in \code{data}
}
\description{
Skill-SPM (\verb{estimated-skills/03_skill_spm.R}, and the expanding-window
as-of variant in \code{R/spm_asof.R}) trains on decay-weighted skill features
rather than raw box-score aggregates, so its offense/defense hand-curated
column lists are a DIFFERENT (smaller) set than the box-score SPM's
(\verb{05_spm.R} / \code{.spm_opta_predictor_cols()}) -- some raw box columns (e.g.
\code{hit_woodwork_p90}, \code{att_pen_goal_p90}) aren't carried as skill features.
Extracted to ONE place so the all-history fit (\verb{03_skill_spm.R} section
10) and the expanding-window per-year fits (\code{fit_expanding_skill_spm()})
can never drift apart -- hand-copied O/D feature lists are a recurring
drift bug in this repo (see \code{.spm_opta_predictor_cols()}'s own history).
}
\keyword{internal}
