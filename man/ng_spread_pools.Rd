% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_spread_pools}
\alias{ng_spread_pools}
\title{Spread team pools across the players who were on the pitch}
\usage{
ng_spread_pools(
  pay,
  actions,
  lineups,
  dacts_share = 0.5,
  keeper_pool_blame = 1,
  keeper_pool_credit = 1,
  keeper_outside_weight = 0,
  dacts_measure = c("act_value", "count", "named_value"),
  verbose = TRUE
)
}
\arguments{
\item{pay}{Payments from \code{ng_build_ledger()}, containing \code{pool_off} /
\code{pool_def} rows.}

\item{actions}{The action table, for the minute each pool was generated.
Needs \code{match_id}, \code{action_id}, \code{time_seconds}.}

\item{lineups}{Opta lineups with \code{match_id}, \code{player_id}, \code{team_id},
\code{is_starter}, \code{sub_on_minute}, \code{sub_off_minute}.}

\item{dacts_share}{How much of the \strong{defensive pool's credit half} to route
by each player's defensive work rather than flat. 0 is a flat spread; 1
routes it entirely by defensive work. Default \strong{0.5} (Pete, 2026-09-21). See the note below on why
only the credit half, and why the default is 0.}

\item{keeper_pool_blame, keeper_pool_credit}{A goalkeeper's weight in the
\strong{defensive pool's} blame and credit halves, relative to an outfield
player's 1. Both default 1. Needs \code{position} in \code{lineups} to find keepers.}

\item{keeper_outside_weight}{A goalkeeper's weight in any pool for play
OUTSIDE his own third. Default 0: a keeper shares unnamed value near his
own goal, not at the other end. Needs \code{start_x} and \code{team_id} on
\code{actions}; without them every action counts as his own third.}

\item{dacts_measure}{What "defensive work" means.
\code{"act_value"} (the default) weights each act by how much its own
\code{epv_delta} moved the game, so a clearance off the line outweighs one on
the halfway line. \code{"count"} counts \code{NG_DEFENSIVE_ACTIONS} instead, treating
every act alike. \code{"named_value"} uses the player's named defensive payments
-- \strong{measured and rejected}, see \code{.ng_defensive_value()}.

\code{"act_value"} is the default because it is the only one that stays monotone
without overshooting. Swept on ENG 2024-2025 at \code{dacts_share} 0 to 1, the
spread across all six positions falls 0.221 -> 0.061 and no position ever
becomes an outlier (the keeper lands mid-pack at +0.019). \code{"count"} flips
defenders above strikers at 1 and drives the keeper to -0.069;
\code{"named_value"} sends him to +0.422. That is a behaviour argument, not a
spread-minimising one -- the spread is not a validated target.}

\item{verbose}{Print a summary. Default \code{TRUE}.}
}
\value{
The same payments with every pool row replaced by one row per player
who was on the pitch, tagged \code{role = "pool_off_spread"} /
\code{"pool_def_spread"}. Pools whose team has no lineup are left unspread and
reported, never silently dropped.
}
\description{
A pool payment has a team but no player: it is the pressure, the marking and
the runs nobody is named for. Measured on ENG 2024-2025, the pools carry
\strong{17.4\%} of absolute ledger value under \code{convention = "margin"} and
\strong{39.1\%} under \code{convention = "team"} (which books a conceding half that is
mostly unnamed). Leaving them unspread would read as that share of football
being done by nobody, and would make any comparison between positions
meaningless. Every figure in this file is ENG 2024-2025 unless it says
otherwise, and the convention is named wherever it changes the number.
}
\details{
The spread is \strong{flat across the eleven on the pitch at that minute}. That is
a deliberate starting point, not a result. Torp swept the alternatives and
found that routing by positional mirror alone made the forward/defender gap
\emph{wider}, because a midfielder's mirror is another midfielder; and that its
richer "context" spread (pairings, defensive acts, mirror, time on ground)
narrowed the gap but repeated worse year over year than a flat spread. Flat
is the option that cannot quietly encode a prior about who deserves it.

Substitutions are honoured to the minute, which matters more in football than
in AFL: a player who came on in the 80th minute is credited only for pools
generated after he came on.
}
\seealso{
Other net_goals: 
\code{\link{ng_adjust_for_rating}()},
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_fold_unpublished}()},
\code{\link{ng_player_game}()},
\code{\link{ng_reconcile_margin}()},
\code{\link{ng_shares}()}
}
\concept{net_goals}
