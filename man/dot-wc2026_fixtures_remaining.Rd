% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/knockout_model.R
\name{.wc2026_fixtures_remaining}
\alias{.wc2026_fixtures_remaining}
\title{How many WC 2026 matches are still unplayed?}
\usage{
.wc2026_fixtures_remaining(cache_dir)
}
\arguments{
\item{cache_dir}{Predictions cache directory (holds \verb{07_predictions.rds}).}
}
\value{
Integer count of WC 2026 rows still marked \code{"fixture"} and carrying
both team names, or \code{NA_integer_}
when the cache file is absent or lacks the columns to answer. \code{NA} means
"cannot tell" and callers should leave their configuration alone rather
than treating it as zero.
}
\description{
Liveness test for the World Cup pipeline branch (steps 11/12/12b/12c in
\code{run_predictions_opta.R}). Those steps simulate a tournament in progress;
once the final is played there is nothing to simulate and
\code{\link[=build_knockout_lookup]{build_knockout_lookup()}} aborts, because its constant-aggregates invariant
only holds while every WC row is an unplayed fixture carrying a single
as-of snapshot. Played rows carry per-match aggregates.
}
\details{
Reads step 07's prediction cache rather than the step 04 match dataset:
same answer, ~53k x 15 instead of ~47MB, in a pipeline with a history of
memory cliffs (panna#128).
}
\keyword{internal}
