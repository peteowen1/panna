% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_reconcile_margin}
\alias{ng_reconcile_margin}
\title{Force each team's published rows to sum to that team's own goal difference}
\usage{
ng_reconcile_margin(ng, fixtures, verbose = TRUE)
}
\arguments{
\item{ng}{Per-game frame from \code{\link[=ng_player_game]{ng_player_game()}}, optionally already folded
by \code{\link[=ng_fold_unpublished]{ng_fold_unpublished()}}. Needs \code{match_id}, \code{player_id}, \code{team_id},
\code{minutes_played} and \code{net_goals}, plus the two halves under either the
\verb{epv_*} or the \verb{ng_*} spelling.}

\item{fixtures}{Fixtures with \code{match_id}, \code{home_team_id}, \code{away_team_id},
\code{home_score}, \code{away_score}.}

\item{verbose}{Print the before-and-after error. Default \code{TRUE}.}
}
\value{
\code{ng} with \code{net_goals} and the defensive half adjusted, plus an
\code{ng_recon} column holding what each player was given, so a reader tracing
one number by hand can see the three parts separately.
}
\description{
The last gap. After \code{\link[=ng_fold_unpublished]{ng_fold_unpublished()}} a match's two sides cancel to
rounding, but neither side lands on the scoreline: measured on ENG 2024-2025
the median team-match is \strong{0.20 goals} from its own goal difference and the
worst is 1.39. The ledger is antisymmetric by construction and anchored to
nothing, so what it tracks is the EPV the actions generated, not the goals
the match actually produced. Restarts, half-time, and the event types SPADL
does not carry all move the state without booking a payment, and that
difference has to go somewhere or the metric is not net goals.
}
\details{
\strong{torp does exactly this and it is why torp reaches 0.000.}
\code{.np_team_margin()} computes \code{short = want - tot} per team-match and spreads
it across the side by time on ground. This is the same step in goals.
\subsection{Why this is safe here and the thing torp rejected was not}{

torp's own docs reject a reconciliation, and the rejection is of a different
step: \code{.np_reconcile(level = "half_margin")}, measured \emph{before} the team-sum
convention, whose residual was \strong{102\% of} \verb{|net_points|} and reordered
players against time on ground. That is a correction larger than the thing it
corrects. Sized the same way before this was written, panna's is not:

\tabular{lr}{
residual as a share of total \verb{|net_goals|} \tab 7.6\% \cr
correlation, per player-game, before vs after \tab 0.9972 \cr
Spearman on season totals \tab 0.9944 \cr
correlation of the residual with minutes \tab 0.0008
}

Nobody meaningfully reorders and the residual carries no minutes bias, so it
is not quietly paying whoever was on the pitch longest. It is in the same
range as the \code{sum}-level reconciliation torp actually ships (3\%).
}

\subsection{Where it lands in the offence/defence split}{

All of it on the defensive half, which is what torp does (it books the change
against the pool channel, "the honest home for the difference"). The gap is
unattributed team-level value, and panna's team pool is already
defence-weighted. Splitting it across both halves pro-rata was the
alternative and it explodes on a player whose two halves nearly cancel --
the same failure torp records from rescaling components.
}
}
\seealso{
Other net_goals: 
\code{\link{ng_adjust_for_rating}()},
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_fold_unpublished}()},
\code{\link{ng_player_game}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
