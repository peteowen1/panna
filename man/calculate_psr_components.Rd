% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psr.R
\name{calculate_psr_components}
\alias{calculate_psr_components}
\title{Calculate PSR with Offensive/Defensive Decomposition}
\usage{
calculate_psr_components(
  skills,
  coef_df,
  osr_coef_df,
  dsr_coef_df,
  center = TRUE
)
}
\arguments{
\item{skills}{A data.table/data.frame with player skill estimates,
containing identity columns (\code{player_id}, \code{player_name}) and
numeric skill columns matching the \code{stat_name} values in \code{coef_df}.}

\item{coef_df}{A data.frame with columns \code{stat_name} and \code{beta}.
If an \code{sd} column is present, each skill is divided by its SD before
multiplying by beta (i.e. the coefficients are on the standardized scale).}

\item{osr_coef_df}{Coefficient data.frame for the offensive model (same
format as \code{coef_df}: columns \code{stat_name}, \code{beta},
optionally \code{sd}).}

\item{dsr_coef_df}{Coefficient data.frame for the defensive model.}

\item{center}{Logical. If TRUE (default), subtract the league mean so
PSR = contribution above average player.}
}
\value{
A data.table with columns: identity columns, \code{psr_raw},
\code{psr}, \code{osr}, \code{dsr}.
}
\description{
Computes the margin-based PSR (best single predictor of match outcomes),
then decomposes it into offensive (OSR) and defensive (DSR) components using
separately trained coefficient models. The decomposition uses an additive
shift so that \code{osr + dsr = psr} exactly.
}
\keyword{internal}
