% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ng_breakdown.R
\name{.ng_breakdown_players}
\alias{.ng_breakdown_players}
\title{Season totals of the net goals breakdown, one block of rows per player}
\usage{
.ng_breakdown_players(bd)
}
\arguments{
\item{bd}{\code{\link[=.ng_breakdown]{.ng_breakdown()}} rows for one season: \code{match_id}, \code{player_id},
\code{category}, \code{value}.}
}
\value{
data.table sorted by \code{player_id}: \code{player_id}, \code{category}, \code{value}
(season total, goals), \code{games} (matches with a breakdown) and \code{net_goals}
(the player's season total, the same on each of their rows, so the page can
check its parts add up).
}
\description{
The player page shows a season's EPV by play type for one player. Reading the
per-match breakdown for that took 13.7 s on a past season (2.4M rows, 21 MB),
because the page downloads every player's matches to draw one. This sums each
player's season across every competition they played, and the file is written
sorted by player in small row groups so the page reads one group.
}
\keyword{internal}
