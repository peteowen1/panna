% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scrape_fbref_parquet.R
\name{build_all_parquet}
\alias{build_all_parquet}
\title{Build all parquet files}
\usage{
build_all_parquet(
  table_types = NULL,
  leagues = NULL,
  seasons = NULL,
  verbose = TRUE
)
}
\arguments{
\item{table_types}{Character vector of table types to process.
Default: all standard table types.}

\item{leagues}{Optional character vector of leagues to process.
If NULL, processes all leagues found.}

\item{seasons}{Optional character vector of seasons to process.
If NULL, processes all seasons found.}

\item{verbose}{Print progress messages}
}
\value{
Data frame with table_type, league, season, n_matches, size_mb columns
}
\description{
Iterates through all table_type/league/season combinations and creates
parquet files. Discovers combinations from both existing RDS files and
existing parquet files (for incremental updates on CI).
}
\examples{
\dontrun{
# Build all parquet files
build_all_parquet()

# Build only summary for England
build_all_parquet(table_types = "summary", leagues = "ENG")
}
}
