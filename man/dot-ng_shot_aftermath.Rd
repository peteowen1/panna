% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{.ng_shot_aftermath}
\alias{.ng_shot_aftermath}
\title{Price a shot at more than its xG: the value it leaves behind}
\usage{
.ng_shot_aftermath(dt, fit = NULL, verbose = TRUE)
}
\arguments{
\item{dt}{The ledger's actions (modified by reference), with \code{epv},
\code{epv_delta}, \code{result}, \code{team_id}, \code{action_type}.}

\item{fit}{Optional: a fit returned by an earlier call (\code{intercept},
\code{slope}), used instead of fitting on \code{dt}.}

\item{verbose}{Print the fit.}
}
\value{
\code{list(dt, fit)}; \code{fit} is \code{NULL} when fewer than 50 non-goal shots
exist, and the shots are then left at their xG.
}
\description{
A shot that does not score still leaves its side something: a corner, a
rebound, the ball. On ENG 2024-2025 the state straight after a non-goal shot
is worth +0.034 goals to the shooting side on average (8,690 shots), against
a mean xG of 0.112. Pricing the shot at its xG alone under-pays the pass
before it and the decision to shoot, and hands that value to whoever touches
the ball next.
}
\details{
Here a shot is worth \code{V0 = xG + (1 - xG) * A}, where \code{A} is a straight line in
xG fitted on the season's own non-goal shots: the value of the next state to
the shooting side, or 0 when nothing follows in the period. It is refitted on
every call, as torp reprices stoppages per season. The row before a shot
targeted its xG, so it moves up by \code{V0 - xG}; a shot that does not score ends
at the next row's start value in its own frame; a goal still ends at 1.
Own goals are left alone. \code{shot_xg} keeps each repriced shot's xG, which
the credit rules need to split the row.
}
\keyword{internal}
