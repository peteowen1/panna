% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_check_team_totals}
\alias{ng_check_team_totals}
\title{Check each team's players against that team's own goal difference}
\usage{
ng_check_team_totals(pay, fixtures, verbose = TRUE)
}
\arguments{
\item{pay}{Payments from \code{ng_build_ledger(convention = "team")}}

\item{fixtures}{Fixtures with \code{match_id}, \code{home_team_id}, \code{home_score},
\code{away_score}}

\item{verbose}{Print the summary. Default \code{TRUE}.}
}
\value{
One row per team-match: \code{own_total}, \code{own_gd}, \code{err}.
}
\description{
The team convention's identity: for a 3-1 win the winners sum to +2 and the
losers to -2, and the match sums to zero. Unlike the margin convention this
is checked per team, not per match.
}
\details{
Like \code{ng_check_conservation()} this is a report, not a gate. Forcing a team
to its goal difference would charge a residual and, as torp measured for the
analogous \code{half_margin} mode, a residual can be larger than the thing it
corrects and can reorder players.
}
\seealso{
Other net_goals: 
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
