% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minutes_model.R
\name{build_minutes_training_data}
\alias{build_minutes_training_data}
\title{Build training dataset for the minutes-projection model}
\usage{
build_minutes_training_data(
  lineups,
  intl_comps,
  ratings_path = "data-raw/cache-opta/07_seasonal_ratings.rds",
  min_team_matches = 5L,
  verbose = TRUE
)
}
\arguments{
\item{lineups}{Data.table of opta lineups (full, not just intl).}

\item{intl_comps}{Character vector of competition codes treated as
"international". Training rows are restricted to these.}

\item{ratings_path}{Path to seasonal xRAPM RDS.}

\item{min_team_matches}{Drop teams with fewer than this many intl matches.}

\item{verbose}{Logical.}
}
\value{
A data.table with features + \code{minutes_played} target. The vector
of training feature column names is in \code{attr(result, "feature_cols")}.
}
\description{
For each (player_id x match) in the international competitions, computes
backward-looking features as of the match date and pairs them with the
realized \code{minutes_played} (the supervised target).
}
\seealso{
Other expected minutes: 
\code{\link{build_team_expected_minutes}()},
\code{\link{classify_role}()},
\code{\link{fit_minutes_model}()},
\code{\link{predict_minutes}()},
\code{\link{prepare_minutes_cache}()},
\code{\link{query_minutes_features}()},
\code{\link{weight_rating_by_minutes}()}
}
\concept{expected minutes}
