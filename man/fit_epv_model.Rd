% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_model.R
\name{fit_epv_model}
\alias{fit_epv_model}
\alias{fit_epv_scoring_model}
\alias{fit_epv_conceding_model}
\title{Fit EPV Model}
\usage{
fit_epv_model(
  features,
  labels,
  method = c("goal", "xg"),
  nfolds = 5,
  max_depth = 6,
  eta = 0.05,
  subsample = 0.8,
  colsample_bytree = 0.8,
  nrounds = 1000,
  early_stopping_rounds = 50,
  verbose = 1
)

fit_epv_scoring_model(...)

fit_epv_conceding_model(...)
}
\arguments{
\item{features}{Data frame from create_epv_features()}

\item{labels}{Data frame with labels (next_goal_label for "goal", next_xg_label for "xg")}

\item{method}{Either "goal" (multinomial) or "xg" (regression). Default "goal".}

\item{nfolds}{Number of CV folds (default 5)}

\item{max_depth}{Maximum tree depth (default 6)}

\item{eta}{Learning rate (default 0.05)}

\item{subsample}{Row subsampling (default 0.8)}

\item{colsample_bytree}{Column subsampling (default 0.8)}

\item{nrounds}{Maximum boosting rounds (default 1000)}

\item{early_stopping_rounds}{Early stopping patience (default 50)}

\item{verbose}{Print progress (default 1)}

\item{...}{Arguments passed to deprecated wrappers (for internal use)}
}
\value{
Fitted EPV model with metadata
}
\description{
Trains an XGBoost model to predict expected possession value.
Supports two methods:
\itemize{
\item "goal": Multinomial classification (who scores next: team/opponent/nobody)
\item "xg": Regression on signed xG of next shot (+team, -opponent, 0 if none)
}
}
