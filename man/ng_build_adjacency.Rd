% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_build_adjacency}
\alias{ng_build_adjacency}
\title{Build a full-stream adjacency table}
\usage{
ng_build_adjacency(events, verbose = TRUE)
}
\arguments{
\item{events}{Raw Opta events, as returned by \code{load_opta_match_events()}.
Needs \code{match_id}, \code{event_id}, \code{type_id}, \code{team_id}, \code{player_id},
\code{period_id}, \code{minute}, \code{second}.}

\item{verbose}{Print a summary of what the table found. Default \code{TRUE}.}
}
\value{
A data.table keyed on \code{match_id} + \code{event_id}, one row per event,
with:
\describe{
\item{next_team_id}{team on the next \emph{visible} event (attribution types
and real actions; markers are skipped)}
\item{next_player_id}{player on that event}
\item{next_type_id}{its Opta type id}
\item{gap_type_id}{if the next visible event is one of
\code{NG_ATTRIBUTION_TYPES}, its id -- otherwise \code{NA}. This is the row SPADL
drops and the ledger would otherwise step over.}
\item{gap_player_id}{the player named on that dropped row}
\item{true_possession_change}{next visible event belongs to the other
team}
}
}
\description{
Answers "what happened next" from the \strong{complete} Opta event stream, before
any SPADL filtering, and returns it keyed on \code{event_id} so the ledger can join
it to SPADL actions via \code{original_event_id}.
}
\details{
Nothing about the EPV model changes: \code{convert_opta_to_spadl()}, the chains,
the features and \code{calculate_action_epv()} all keep seeing exactly what they
see today. Only the ledger's view of "who was next" is corrected. This is
deliberate -- moving the filter itself would shift the model's inputs and
every measurement taken against them.
}
\seealso{
Other net_goals: 
\code{\link{ng_adjust_for_rating}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_fold_unpublished}()},
\code{\link{ng_player_game}()},
\code{\link{ng_reconcile_margin}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
