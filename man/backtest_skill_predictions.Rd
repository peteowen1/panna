% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimated_skills.R
\name{backtest_skill_predictions}
\alias{backtest_skill_predictions}
\title{Backtest skill predictions against actual match performance}
\usage{
backtest_skill_predictions(
  match_stats,
  decay_params = NULL,
  stat_cols = NULL,
  min_history = 5,
  sample_n = NULL,
  seed = 42
)
}
\arguments{
\item{match_stats}{A data.table from \code{compute_match_level_opta_stats()}.}

\item{decay_params}{Decay parameters (with optional \code{stat_priors}).}

\item{stat_cols}{Character vector of stats to evaluate. If NULL, uses a
default set of key stats.}

\item{min_history}{Minimum prior matches before predictions count (default 5).}

\item{sample_n}{Max players to sample for speed (default NULL = all).}

\item{seed}{Random seed for sampling.}
}
\value{
A list with:
\describe{
\item{predictions}{data.table of individual predictions: player_id,
match_date, stat, predicted, actual, minutes}
\item{accuracy}{data.table of per-stat accuracy metrics: RMSE,
improvement vs simple average, improvement vs last match}
}
}
\description{
For each player-match in the data, computes the Bayesian skill estimate
using all prior matches (with decay weighting), then compares to actual
performance. This is equivalent to computing weekly skill estimates and
evaluating how well they predict the next match.
}
