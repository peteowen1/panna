% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_asof.R
\name{fit_expanding_skill_spm}
\alias{fit_expanding_skill_spm}
\title{Fit expanding-window skill-SPM O/D models for one reference year}
\usage{
fit_expanding_skill_spm(
  skill_features,
  pooled_rapm_ratings,
  cutoff_year,
  nfolds = 5
)
}
\arguments{
\item{skill_features}{\verb{02_skill_features.rds} (one row per player-season,
needs \code{player_id}, \code{season_end_year}, \code{total_minutes}).}

\item{pooled_rapm_ratings}{Output of \code{fit_expanding_pooled_rapm()$ratings}
for the SAME \code{cutoff_year} (needs \code{player_id}, \code{offense}, \code{defense}).}

\item{cutoff_year}{Integer; skill features from seasons \verb{< cutoff_year} only.}

\item{nfolds}{CV folds for both glmnet and xgboost (default 5).}
}
\value{
List with \code{offense_spm_glmnet}, \code{offense_spm_xgb},
\code{defense_spm_glmnet}, \code{defense_spm_xgb}, \code{offense_spm_ratings},
\code{defense_spm_ratings} (same shape as \verb{03_skill_spm.rds}), \code{cutoff_year},
\code{n_train}. \code{NULL} (with a warning) if fewer than 100 players are
available to train on (e.g. the earliest season, no prior data).
}
\description{
Step B of the as-of fix: skill features are filtered to seasons strictly
before \code{cutoff_year} (an honest, point-in-time feature set), the
most-recent-per-player slice is taken (mirrors
\verb{data-raw/estimated-skills/03_skill_spm.R}), and offense/defense
elastic-net + XGBoost SPM models are fit against the matching
\code{fit_expanding_pooled_rapm()} target -- so NEITHER the features NOR the
RAPM target the SPM predicts can see season \code{cutoff_year} or later.
Column sets come from the shared \code{.skill_spm_offense_cols()} /
\code{.skill_spm_defense_cols()} / \code{.skill_spm_defense_constraints()} (the same
definitions the all-history fit in \verb{03_skill_spm.R} section 10 uses, so
the two can never drift apart).
}
\keyword{internal}
