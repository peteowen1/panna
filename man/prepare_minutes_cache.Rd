% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minutes_query.R
\name{prepare_minutes_cache}
\alias{prepare_minutes_cache}
\title{Prepare a cached lineups data object for fast minutes-feature queries}
\usage{
prepare_minutes_cache(
  lineups,
  intl_comps,
  ratings_path = "data-raw/cache-opta/07_seasonal_ratings.rds",
  cache_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{lineups}{Data.frame/data.table of opta lineups (full, both intl and club).}

\item{intl_comps}{Character vector of competition codes treated as
"international".}

\item{ratings_path}{Path to seasonal xRAPM RDS (for panna lookups).}

\item{cache_path}{Optional RDS file path. If supplied, saves the result.}

\item{verbose}{Logical.}
}
\value{
A list with:
\itemize{
\item \code{per_player_history} -- keyed data.table per (player_id, date_int)
with \verb{mins_intl, mins_club, app_intl, start_intl, start_club, is_intl}
\item \code{cumsum_lookup} -- per-player precomputed cumulative-sum arrays for
fast rolling-window queries
\item \code{modal_role} -- modal non-Substitute role per player
\item \code{player_panna} -- panna ratings table (player_id x season_end_year)
\item \code{team_rotation} -- global rotation_idx per team
\item \code{team_intl_count} -- number of intl matches per team
}
}
\description{
Runs the global / per-team / per-player passes once and caches the
intermediate data structures needed by \code{query_minutes_features()}.
Re-run only when the underlying lineups parquet changes.
}
