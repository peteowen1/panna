% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rating.R
\name{fit_bt_ratings}
\alias{fit_bt_ratings}
\title{Fit Bradley-Terry-Davidson team ratings}
\usage{
fit_bt_ratings(predictions, neutral = FALSE, max_iter = 200L, verbose = TRUE)
}
\arguments{
\item{predictions}{Data frame with columns \code{home_team}, \code{away_team},
\code{prob_H}, \code{prob_D}, \code{prob_A}.}

\item{neutral}{Logical or vector of same length as \code{nrow(predictions)}.
If TRUE, the home-field advantage parameter is set to zero for that row.
Default FALSE (every row treated as a home/away pair).}

\item{max_iter}{Integer. L-BFGS-B max iterations. Default 200.}

\item{verbose}{Logical. Print fit diagnostics. Default TRUE.}
}
\value{
A list with:
\itemize{
\item \code{ratings}: data frame of team / rating / rank
\item \code{home_adv}: scalar home-advantage parameter (log-odds)
\item \code{nu}: draw-frequency parameter
\item \code{loss}: final cross-entropy loss
\item \code{converged}: optim convergence flag
}
}
\description{
Given match-level W/D/L probabilities (from the prediction pipeline), fit a
single strength rating per team. Uses the Davidson (1970) extension that
folds draws into Bradley-Terry: \verb{P(draw) prop nu.exp((r_i+r_j)/2)}.
}
\details{
Optimisation: minimise cross-entropy between predicted probs and BT-implied
probs via L-BFGS-B. Ratings are centered to mean zero for interpretability.
}
\seealso{
Other world cup simulation: 
\code{\link{bt_match_prob}()},
\code{\link{build_knockout_lookup}()},
\code{\link{compute_league_offsets}()},
\code{\link{match_is_international}()},
\code{\link{mirror_match_rows}()},
\code{\link{run_wc2026_reference_checks}()},
\code{\link{simulate_world_cup}()}
}
\concept{world cup simulation}
