% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opta_loaders.R, R/piggyback.R
\name{pb_download_opta}
\alias{pb_download_opta}
\title{Download Opta Data to Local Directory}
\usage{
pb_download_opta(
  dest = NULL,
  repo = "peteowen1/pannadata",
  tag = "opta-latest",
  pattern = NULL,
  check_timestamp = FALSE,
  force = FALSE,
  dry_run = FALSE,
  verbose = TRUE
)

pb_download_opta(
  dest = NULL,
  repo = "peteowen1/pannadata",
  tag = "opta-latest",
  pattern = NULL,
  check_timestamp = FALSE,
  force = FALSE,
  dry_run = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{dest}{Destination directory (default: the Opta data dir,
\code{opta_data_dir()}). Files are written flat into this directory,
matching the consolidated layout the loaders read.}

\item{repo}{GitHub repository in "owner/repo" format.}

\item{tag}{Release tag (default \code{"opta-latest"}).}

\item{pattern}{Optional regex on asset names to restrict the sync
(e.g. \code{"^events_|^opta_"}). \code{NULL} considers all assets.}

\item{check_timestamp}{Logical. If \code{TRUE}, also re-download assets whose
release timestamp is newer than the local file even when the size matches
(stricter; catches same-size content changes but re-pulls files that were
merely re-uploaded). Default \code{FALSE} — size + presence only, which is
the efficient "what actually changed" sync for these append-growing files.}

\item{force}{Re-download every matching asset regardless of local state.}

\item{dry_run}{Report what would be downloaded without downloading.}

\item{verbose}{Print per-file status.}
}
\value{
Invisibly returns the path to the installed data.

Invisibly, a data.frame with one row per asset: \code{file_name},
\code{action} ("download"/"skip"), \code{reason}, and \code{size}.
}
\description{
Downloads consolidated Opta parquet files from GitHub releases to the local
pannadata/data/opta/ directory. Match events are excluded (too large for a
single file) - use \code{load_opta_match_events(source = "remote")} to load them
on-demand from per-league release files.

Syncs a GitHub release to a local directory by downloading only the assets
that are \strong{missing}, a \strong{different size}, or \strong{updated more recently} on
the release than the local copy. Avoids re-pulling the full multi-GB dataset
when only a few consolidated files changed (e.g. the daily Opta scrape
refreshes ~10 of ~125 assets). Unlike \code{\link{pb_download_source}} it does
not require a tarball asset — it operates on the individual release files.
}
\examples{
\dontrun{
# Download latest Opta data to pannadata/data/opta/
pb_download_opta()

# Then load with:
load_opta_stats("ENG", season = "2024-2025")
}
\dontrun{
# See what's out of date without downloading
pb_download_opta(dry_run = TRUE)

# Pull only the changed consolidated files
pb_download_opta()

# Just the event files
pb_download_opta(pattern = "^events_")
}
}
