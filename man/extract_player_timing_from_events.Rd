% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splint_creation.R
\name{extract_player_timing_from_events}
\alias{extract_player_timing_from_events}
\title{Extract Season from Match Date}
\usage{
extract_player_timing_from_events(match_events)
}
\arguments{
\item{match_events}{Raw Opta match-events data frame with columns
\code{match_id}, \code{type_id}, \code{period_id}, \code{team_id}, \code{player_id}, \code{minute},
\code{second}, \code{qualifier_json}.}
}
\value{
Data frame with columns \code{match_id}, \code{player_id}, \code{team_id},
\code{is_starter}, \code{on_minute}, \code{off_minute}. Bench players who never came on
are omitted. Returns empty data frame if input is empty or missing
required columns.
}
\description{
Extract Per-Player On/Off Times from Raw Opta Match Events
}
\details{
Derives \code{on_minute} and \code{off_minute} for every player in every match
directly from Opta event data -- no reliance on lineup minute counts.
Uses second-level precision throughout.

Sources:
\itemize{
\item Starting XI: \code{type_id == 34} (formation/squad set), qualifier 30 = player IDs,
qualifier 131 = position number (1-11 = starter, 0 = bench).
\item Sub on: \code{type_id == 19} (Player On).
\item Sub off: \code{type_id == 18} (Player Off).
\item Red card off: \code{type_id == 17} (Card) with qualifier 33 (red) or 14 (second yellow).
\item Match end: \code{type_id == 30} with \code{period_id == 2} (used as default off_minute
for finishers who never came off).
}

Why this beats lineups: Opta records \code{minutes_played = 90} for unsubstituted
finishers regardless of stoppage time, and rounds sub timing to whole minutes.
Chains carry the real second-precision times.
}
