% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_player_game}
\alias{ng_player_game}
\title{Aggregate net goals to one row per player-match}
\usage{
ng_player_game(pay, lineups, verbose = TRUE)
}
\arguments{
\item{pay}{Payments from \code{ng_spread_pools()}.}

\item{lineups}{Opta lineups, for minutes, names, date, league and season.}

\item{verbose}{Print a summary. Default \code{TRUE}.}
}
\value{
One row per player-match: identifiers, \code{minutes_played},
\code{net_goals}, \code{epv_offensive}, \code{epv_defensive}, and one \verb{ng_*} column per
role so a rating layer can choose its own grouping rather than inheriting
this one (torp D3: store tags, group later).
}
\description{
Turns the payment ledger into the frame the rating layer consumes. The
column contract deliberately matches \code{aggregate_player_game_epv()}'s --
\code{player_id}, \code{player_name}, \code{match_date}, \code{minutes_played},
\code{epv_offensive}, \code{epv_defensive} -- so \code{calculate_epr_regression()} can be
pointed at either without changing, and the two can be gated against each
other on identical footing.
}
\details{
\strong{The offence/defence split means something different here, and it is the
better of the two.} \code{aggregate_player_game_epv()} splits by bucketing action
types (passing and shooting are offensive, tackles and keeper handling
defensive), which is presentational -- re-bucketing an action changes the
split and not the total. Net goals splits by which half of the double entry
the payment sits on: \code{offence} is the side that acted, \code{defence} the side
that conceded. Every player is paid on both halves of every action his team
is involved in, so a defender who never touches the ball still has a
defensive number, which is the whole point.

Pools must be spread first. A payment with no player is real value, and
dropping it here would quietly shrink a player-game total while every team
total stayed correct -- so an unspread pool aborts rather than being skipped.
}
\seealso{
Other net_goals: 
\code{\link{ng_adjust_for_rating}()},
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_fold_unpublished}()},
\code{\link{ng_reconcile_margin}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
