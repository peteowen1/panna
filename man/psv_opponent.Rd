% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psv_opponent.R
\name{psv_opponent}
\alias{psv_opponent}
\title{Opponent adjustment for per-match PSV (panna#220)}
\description{
PSV scores a player's box-score line for a single match. Nothing in that line
knows who the opposition was, so a defender facing a bottom-of-the-table
attack and one facing Manchester City are priced identically. PSR gets an
opponent control at training time (\verb{07_train_psr_model.R} passes the
opponents' defensive ratings as unpenalized regressors) and EPR gets one in
its ridge (\code{opp_def_rating}); PSV alone had none.
}
\details{
The adjustment is additive and deliberately minimal:

\code{psv_adj = psv - gamma * opp_def_rating}

\code{opp_def_rating} is the opposing team's season defensive strength from
\code{cache-opta/team_season_strength.parquet}, in the xRAPM sign convention where
\strong{negative is good defence}. So facing a strong defence (negative rating)
raises the adjusted value and facing a leaky one lowers it, for \code{gamma > 0}.
}
\section{Why a single global gamma}{

Per-position or per-league gammas were not fitted. The relationship being
estimated is "how much of a player's box output is explained by the
opposition", which has no strong prior reason to differ by position, and the
project's experience with splitting a calibration across two axes at once is
that the joint cells lose power long before the marginals do (see
\code{docs/reference/RATING_CALIBRATION.md} on the position x season grid). Start
with the marginal; split it only if a residual check demands it.
}

\section{Not enabled by default}{

This changes PSV, and PSV is the input to \code{build_league_network()}, which
produces the league offsets, which are added to PSR. Turning it on therefore
moves three things at once, and a comparison arm that differs on more than one
axis measures nothing. It must be validated as its own single-axis change
against a re-run baseline, not bundled into another retrain.
}

\seealso{
Other psr: 
\code{\link{PSV_RELIABILITY_GD_SCALE}},
\code{\link{apply_psv_calibration}()},
\code{\link{apply_psv_opponent_adjustment}()},
\code{\link{calculate_psr}()},
\code{\link{calculate_psv}()},
\code{\link{calculate_psv_components}()},
\code{\link{compute_player_psv}()},
\code{\link{default_stat_rating_params}()},
\code{\link{fit_psv_opponent_adjustment}()},
\code{\link{load_opta_psr_weekly}()},
\code{\link{load_psv_match_reliability}()},
\code{\link{player_psr}()},
\code{\link{soccer_position_map}()},
\code{\link{soccer_stat_rating_definitions}()},
\code{\link{stat_rating_names}()}
}
\concept{psr}
