% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shot_context.R
\name{.add_shot_context_features}
\alias{.add_shot_context_features}
\title{Add the pre-shot context a model needs to its shot features}
\usage{
.add_shot_context_features(
  features,
  shots,
  need,
  events = NULL,
  foot_history = NULL,
  what = "xG"
)
}
\arguments{
\item{features}{Shot feature frame, one row per shot in \code{shots} (same order).}

\item{shots}{SPADL shot rows: \code{match_id}, \code{original_event_id}, \code{player_id}.}

\item{need}{Context columns the model reads (from its feature_cols).}

\item{events}{Full Opta events for these matches (see \code{.shot_context()}).
Always required for context inputs: columns already on \code{shots} are not
trusted, since other steps write same-named columns with other meanings.}

\item{foot_history}{\code{.shot_foot_history()} output; needed for \code{foot_share}.}

\item{what}{Model name for messages.}
}
\value{
\code{features} with the \code{need} columns added. Missing values stay NA:
the models were trained with NA meaning "no assist" / "too few earlier
shots", and read it that way (predict_xg honours \code{na_is_missing}).
}
\description{
Add the pre-shot context a model needs to its shot features
}
\keyword{internal}
