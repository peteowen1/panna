% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_season}
\alias{simulate_season}
\title{Simulate Season}
\usage{
simulate_season(
  predictions,
  completed,
  n_sims = 10000L,
  points_win = 3L,
  points_draw = 1L,
  verbose = TRUE
)
}
\arguments{
\item{predictions}{Data frame with columns: \code{home}, \code{away}, \code{prob_H},
\code{prob_D}, \code{prob_A}, and optionally \code{pred_home_goals}, \code{pred_away_goals}.
Only unplayed fixtures should be included.}

\item{completed}{Data frame of completed matches with columns: \code{home},
\code{away}, \code{home_goals}, \code{away_goals}. These results are fixed across
all simulations.}

\item{n_sims}{Integer. Number of Monte Carlo simulations. Default 10000.}

\item{points_win}{Integer. Points for a win. Default 3.}

\item{points_draw}{Integer. Points for a draw. Default 1.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
A list with:
\itemize{
\item \code{table}: Summary table with mean points, title/UCL/relegation probabilities
\item \code{simulations}: Raw simulation results (n_sims x n_teams matrix of points)
\item \code{positions}: Position frequency matrix (n_teams x n_teams)
}
}
\description{
Runs Monte Carlo simulations of remaining fixtures to project final
league standings. Uses match-level win/draw/loss probabilities from
the prediction model.
}
\examples{
\dontrun{
# Predictions for remaining fixtures
preds <- data.frame(
  home = c("Arsenal", "Liverpool"),
  away = c("Chelsea", "Man City"),
  prob_H = c(0.45, 0.35),
  prob_D = c(0.28, 0.30),
  prob_A = c(0.27, 0.35)
)
# Already-played matches
completed <- data.frame(
  home = "Arsenal", away = "Liverpool",
  home_goals = 2, away_goals = 1
)
result <- simulate_season(preds, completed, n_sims = 1000)
result$table
}
}
\concept{season simulation}
