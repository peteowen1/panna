% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_asof.R
\name{assert_prior_free_target}
\alias{assert_prior_free_target}
\title{Abort unless a target artifact is provenance-stamped prior-free RAPM}
\usage{
assert_prior_free_target(target)
}
\arguments{
\item{target}{The candidate target artifact -- a \verb{04b} vintage list, the
top-level \verb{04b} list, or a raw \code{fit_rapm()}/\code{fit_rapm_with_prior()}
model object.}
}
\value{
Invisibly \code{TRUE} if the target is accepted.
}
\description{
Static circularity guard (BOX-SCORE-VALUE-SPM-REDESIGN.md sec 2.4.1):
\code{fit_rapm_with_prior()} (\code{R/rapm_model.R:389}) shrinks toward an SPM
prior, so xRAPM and career panna embed box-stat information -- regressing
box features onto them (directly or via any downstream SPM panel target)
would close the SPM -> prior -> posterior -> "prior-free" target loop.
Any box-score value training entry point (e.g. the planned
\code{fit_spm_panel()}) must call this on its target argument before fitting.
}
\details{
Two accepted shapes: (1) a \verb{04b_rapm_window_targets.R} vintage element (or
the top-level list), stamped \code{target_provenance = "prior_free_rapm_window"}
by that script and nowhere else; (2) a raw \code{fit_rapm()} model object
(legacy path) whose \code{panna_metadata$type == "rapm"} with no \code{used_prior}
field -- \code{fit_rapm_with_prior()} always sets \code{used_prior = TRUE},
\code{fit_rapm()} never sets it, so its absence is the discriminator. Anything
else (including \code{type == "xrapm"}/\code{"xrapm_net"}, or no provenance at all)
aborts.
}
\seealso{
Other rapm: 
\code{\link{create_all_splints}()},
\code{\link{create_rapm_design_matrix}()},
\code{\link{extract_period_end_times}()},
\code{\link{extract_player_timing_from_events}()},
\code{\link{extract_rapm_ratings}()},
\code{\link{fit_rapm}()},
\code{\link{fit_rapm_with_prior}()}
}
\concept{rapm}
