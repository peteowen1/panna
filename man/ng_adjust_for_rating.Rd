% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_adjust_for_rating}
\alias{ng_adjust_for_rating}
\title{Position-centre per-game net goals for the rating layer}
\usage{
ng_adjust_for_rating(player_game, positions, by_season = TRUE, verbose = TRUE)
}
\arguments{
\item{player_game}{Output of \code{ng_player_game()}.}

\item{positions}{A player-to-position map with \code{player_id} and \code{position},
e.g. from \code{get_player_positions()}. Rows whose position is unknown are
centred on the all-player mean rather than dropped, and reported.}

\item{by_season}{Centre within season as well as position. Default \code{TRUE}.}

\item{verbose}{Print a summary. Default \code{TRUE}.}
}
\value{
The same frame with \code{epv_offensive} and \code{epv_defensive} replaced by
their centred values, the originals kept as \verb{*_raw}, and \code{net_goals_raw}
preserved. The column names are deliberately unchanged so this is a
drop-in for \code{calculate_epr_regression()}.
}
\description{
The two-layer split, made explicit. The ledger conserves and therefore
carries no baseline: the moment a positional mean is subtracted, a team's
players stop summing to its goal difference. Predicting is a different job
with different rules, so the centring belongs here, downstream, where
breaking conservation is allowed and useful.
}
\details{
\strong{What this mirrors, and why it is needed for a fair comparison.} Production
EPR is fed \code{epv_offensive_adj} / \code{epv_defensive_adj} renamed to the raw
names (\code{data-raw/match-predictions-opta/build_epr_weekly.R:63-66}) -- the
position-centred columns produced at export by \verb{10b_export_game_logs.R}, not
the raw ones. \code{ng_player_game()} emits raw net goals. Feeding those two to
\code{calculate_epr_regression()} unchanged would compare a centred input against
an uncentred one and attribute the difference to the ledger, which it is not.
This function removes that confound.

Centring is per position \strong{and season}: position means drift between
seasons, and a single pooled mean would carry one season's shape into
another. Measured on ENG 2024-2025 the means run from +0.088 for a striker
to -0.035 for a defender per player-game -- small, real, and exactly the
systematic offset a rating should not reward or punish a player for.
}
\seealso{
Other net_goals: 
\code{\link{ng_build_adjacency}()},
\code{\link{ng_build_ledger}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_player_game}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
