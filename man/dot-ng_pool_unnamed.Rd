% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{.ng_pool_unnamed}
\alias{.ng_pool_unnamed}
\title{Route a payment whose recipient the feed does not name into a team pool}
\usage{
.ng_pool_unnamed(pay)
}
\arguments{
\item{pay}{Payments with \code{player_id}, \code{role}.}
}
\value{
The same payments with unnamed non-pool rows re-tagged as pools.
}
\description{
A payment with no player would be dropped by any per-player aggregation --
silently, because the team totals are untouched by the loss. Both conventions
need this, and for a while only the margin one had it: the team branch
returned before reaching it, so an action with no attributed player produced
an \code{actor} row with \code{player_id = NA} that sat outside \code{ng_spread_pools()}
(which only touches pool roles) and vanished from every player-level rollup.
}
\keyword{internal}
