% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_goals.R
\name{ng_build_ledger}
\alias{ng_build_ledger}
\title{Build the net goals ledger}
\usage{
ng_build_ledger(
  spadl_with_epv,
  adj = NULL,
  fixtures,
  allocate = TRUE,
  shares = ng_shares(),
  convention = c("team", "margin"),
  lineups = NULL,
  shot_chain = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{spadl_with_epv}{Output of \code{calculate_action_epv()}.}

\item{adj}{Output of \code{ng_build_adjacency()}. Optional: when supplied, the
ledger uses the true next actor rather than SPADL's post-filter neighbour.
12.01\% of actions disagree, so leaving it out is a measurably different
ledger, not a convenience.}

\item{fixtures}{Fixtures with \code{match_id}, \code{home_team_id}, \code{away_team_id}.
Used only to pick the home frame, never to fit anything.}

\item{allocate}{Apply the credit and blame rules (\code{TRUE}, the default) or pay
every row whole to its actor (\code{FALSE}). The crude mode exists so the
identity can be asserted with no rule for an error to hide behind.}

\item{shares}{Output of \code{ng_shares()}. Ignored when \code{allocate = FALSE}.}

\item{convention}{\code{"team"} (the default, Pete 2026-09-21) allocates each
action twice, once per side, so \strong{each team} sums to its own goal
difference -- +2 and -2 for a 3-1 win, zero across the match. That is the
ESPN Net Points convention and what torp ships as of 1.7.0.
\code{"margin"} allocates it once, split across both sides, so the \strong{match}
sums to the goal difference and team totals float. Its pools carry 17.4\% of
absolute ledger value against the team convention's 39.1\%, because it never
books the conceding half -- which is most of what nobody is named for. Kept
for comparison.}

\item{lineups}{Optional lineups (\code{match_id}, \code{team_id}, \code{player_id},
\code{position}, \code{sub_off_minute}). Used to name each side's goalkeeper on the
finish step of a shot that has no save row (a goal). Without it, that step
goes to the defending side's pool.}

\item{shot_chain}{If \code{TRUE} (default), the row after a shot starts from 0,
the shot's end, instead of the model's restart value, so no value appears
between them unbooked (309 goals a season on ENG 2024-25). Needs \code{epv} on
the actions; ignored without it.}

\item{verbose}{Print a summary. Default \code{TRUE}.}
}
\value{
A data.table of payments, one row per (action, recipient):
\code{match_id}, \code{action_id}, \code{player_id}, \code{team_id}, \code{is_home}, \code{role},
\code{play_type}, \code{value_home} (home-margin frame), \code{value_own} (the player's
own frame, so positive is always good for him).
}
\description{
Turns per-action \code{epv_delta} into one payment row per (action, recipient), in
the \strong{home-margin frame}: an away team's action is negated so a single number
conserves. Summed over a match, the payments equal the goal difference.
}
\details{
The allocation at this stage is deliberately crude -- every row pays its whole
\code{epv_delta} to the acting player. That is obviously wrong about \emph{who} and
provably right about \emph{how much}, which makes it the one clean moment to assert
the identity: there is no rule for an error to hide behind, and nothing has
yet been forced toward a target. \code{\link[=ng_reconcile_margin]{ng_reconcile_margin()}} does force one, at
the very end of the chain, and that is exactly why this assertion has to
happen HERE and stay here. Torp's suite passed 48 assertions against a ledger
with the away sign flipped because every test ran after its reconciler; this
function is the assertion point that avoids repeating that.

Difficulty terms (the decision/surprise split, D-SHOT, defensive routing)
arrive in step 3 and only ever \emph{subdivide} a row's \code{epv_delta}. They can never
change a match total.
}
\seealso{
Other net_goals: 
\code{\link{ng_adjust_for_rating}()},
\code{\link{ng_build_adjacency}()},
\code{\link{ng_check_conservation}()},
\code{\link{ng_check_team_totals}()},
\code{\link{ng_fold_unpublished}()},
\code{\link{ng_player_game}()},
\code{\link{ng_reconcile_margin}()},
\code{\link{ng_shares}()},
\code{\link{ng_spread_pools}()}
}
\concept{net_goals}
