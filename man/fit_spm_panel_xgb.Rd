% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_panel.R
\name{fit_spm_panel_xgb}
\alias{fit_spm_panel_xgb}
\title{Fit the XGBoost half of the S6 panel SPM (player-grouped CV)}
\usage{
fit_spm_panel_xgb(
  panel,
  target = c("offense", "defense"),
  predictor_cols = NULL,
  weight_transform = c("linear", "sqrt"),
  nfolds = 5,
  seed = 1,
  max_depth = 4,
  eta = 0.1,
  subsample = 0.8,
  colsample_bytree = 0.8,
  nrounds = 500,
  early_stopping_rounds = 20
)
}
\arguments{
\item{panel}{Output of \code{build_spm_panel()} (or a \code{vintage_year}-subset).}

\item{target}{\code{"offense"} or \code{"defense"} (fits \verb{<target>_target}).}

\item{predictor_cols}{Feature columns (default: canonical
\code{.spm_opta_predictor_cols(panel)}).}

\item{weight_transform}{\code{"linear"} (S4a/S6 parity, default) or \code{"sqrt"}.}

\item{nfolds, seed}{Grouped-CV config.}

\item{max_depth, eta, subsample, colsample_bytree, nrounds, early_stopping_rounds}{XGBoost knobs (defaults = the Wave-2-validated panel config, identical
to the 09c/13c bake-off scripts; NB production 05_spm.R's legacy
\code{fit_spm_xgb()} calls use eta=0.02/nrounds=1000 — different model,
different tuning).}
}
\value{
An \code{xgb.Booster} with \code{panna_metadata} (type "spm_panel_xgb",
target, predictor_cols, best_nrounds, cv_rmse).
}
\description{
The panel repeats players across vintages, so \code{fit_spm_xgb()}'s random
\code{xgb.cv} folds would leak players between train/test and overfit the
round count (the plan's R5 hazard). This fit builds its folds with
\code{make_grouped_player_foldid()} (every player in exactly one fold,
asserted) and mirrors \code{fit_spm_panel()}'s weighting/complete-case
handling. Promoted to the package after three script copies
(09c/13c/05_spm integration, 2026-07-22 Wave 4).
}
\seealso{
Other spm panel: 
\code{\link{assert_asof_panel_window}()},
\code{\link{assert_grouped_player_folds}()},
\code{\link{build_spm_panel}()},
\code{\link{classify_role_group}()},
\code{\link{fit_spm_panel}()},
\code{\link{make_grouped_player_foldid}()},
\code{\link{predict_spm_panel}()},
\code{\link{predict_spm_panel_net}()},
\code{\link{predict_spm_panel_xgb}()}
}
\concept{spm panel}
