% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_prediction.R
\name{.disambiguate_collided_team_names}
\alias{.disambiguate_collided_team_names}
\title{Disambiguate team-name collisions before Elo iteration}
\usage{
.disambiguate_collided_team_names(results)
}
\arguments{
\item{results}{As in \code{\link[=compute_match_elos]{compute_match_elos()}}.}
}
\value{
\code{results} with \code{home_team}/\code{away_team} disambiguated where needed,
plus an \code{id_rename_map} attribute (named character vector, possibly
length 0).
}
\description{
\code{compute_match_elos()} keys its internal state by team NAME, so two
genuinely different clubs that happen to share a name (confirmed:
"Arsenal" is both the EPL club and Arsenal de Sarandi in the Argentine
Liga Profesional) blend their match histories into one Elo trajectory --
unlike the panna#204 case (one real club, several name spellings), this
is one real name, several real clubs. Only usable when \code{results} carries
\code{home_team_id}/\code{away_team_id} (a no-op otherwise, so old callers/tests
without those columns see zero behavior change).
}
\details{
For each name shared by >1 distinct \code{team_id}, the identity with the most
rows keeps the plain name (the common case); every other identity sharing
that name gets a \verb{[id:xxxxxxxx]} suffix appended, so the Elo simulation
tracks them as separate teams. This only affects the internal keys used
during iteration and the names returned in \code{final_elos} -- per-match rows
are returned aligned by \code{match_id}/row order regardless, so this is
transparent to any caller that only reads \code{per_match}. A caller doing a
post-hoc name lookup against \code{final_elos} for an UPCOMING fixture (e.g.
step 03's \code{lookup_elo()}) needs the same disambiguation applied to its own
lookup key -- see the returned \code{id_rename_map} (named character vector,
\verb{team_id -> disambiguated name}, empty if no collisions were found).
}
\keyword{internal}
