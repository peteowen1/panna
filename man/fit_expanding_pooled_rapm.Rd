% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spm_asof.R
\name{fit_expanding_pooled_rapm}
\alias{fit_expanding_pooled_rapm}
\title{Fit a pooled RAPM restricted to seasons strictly before a cutoff year}
\usage{
fit_expanding_pooled_rapm(
  rapm_data,
  splint_season_map,
  cutoff_year,
  lambda_formula = function(n) 16.67 * n^(-0.58),
  nfolds = 5,
  seed = NULL
)
}
\arguments{
\item{rapm_data}{The pooled \code{rapm_data} list from \verb{04_rapm.rds$rapm_data}.}

\item{splint_season_map}{data.frame/data.table with \code{splint_id},
\code{season_end_year}.}

\item{cutoff_year}{Integer; only seasons \verb{< cutoff_year} are used to train.}

\item{lambda_formula}{\verb{function(n_obs)} giving the mini-CV grid center
(default the panna#87 sample-size formula, \code{16.67 * n_obs^-0.58}).}

\item{nfolds}{CV folds (default 5, matching the production pooled fit).}

\item{seed}{RNG seed for \code{fit_rapm()}'s CV fold assignment, for
reproducible per-cutoff-year fits. \code{NULL} = no explicit seed.}
}
\value{
List: \code{ratings} (data.frame player_id/rapm/offense/defense),
\code{lambda_min}, \code{n_obs}, \code{cutoff_year}. \code{NULL} (with a warning) if fewer
than 1000 valid observations remain (too few prior seasons).
}
\description{
Mirrors the production step-04 pooled RAPM fit (panna#87 bracketed
mini-CV lambda grid), but on an expanding-window ROW SUBSET of the design
matrix instead of the full history -- the as-of deployment shape
recommended by FABLE-ASOF-EXPERIMENTS.md sec 4.
}
\keyword{internal}
