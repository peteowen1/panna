% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_ratings_epv.R
\name{calculate_epr_regression}
\alias{calculate_epr_regression}
\title{Calculate EPR via weighted ridge regression with league/opponent FE}
\usage{
calculate_epr_regression(
  player_game_epv,
  ref_date = NULL,
  decay = 400,
  alpha = 0,
  lambda = NULL,
  prior_strength = 5,
  verbose = FALSE
)
}
\arguments{
\item{player_game_epv}{Per-game EPV data. Required columns:
\code{player_id}, \code{player_name}, \code{match_date},
\code{minutes_played}, \code{epv_offensive}, \code{epv_defensive}.
Recommended additional columns: \code{league} (or \code{competition}),
\code{season_end_year}, \code{opp_def_rating} (continuous opponent
defensive strength, e.g., from RAPM-derived team ratings).}

\item{ref_date}{Snapshot date — only matches strictly before this are used.}

\item{decay}{Exponential decay constant in days for the time weight.}

\item{alpha}{glmnet mixing parameter (0 = pure ridge, recommended).}

\item{lambda}{Optional lambda. If NULL, uses the median of a 30-lambda
path (good for ridge with sensible default shrinkage).}

\item{prior_strength}{Equivalent-games prior for shrinking small-sample
players toward 0. Implemented by adding "phantom" zero-y rows weighted
by \code{prior_strength} for each player. Set to 0 to disable.}

\item{verbose}{If TRUE, print step timings.}
}
\value{
A data.table with one row per player: \code{player_id},
\code{player_name}, \code{epr}, \code{epr_offensive}, \code{epr_defensive},
\code{n_games}, \code{wt_games}.
}
\description{
Per-(player, game) regression that simultaneously estimates per-player
skill (β_player, returned as EPR) and league/opponent context effects.
Uses exponential time-decay weighting on observations.
}
