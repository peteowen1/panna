% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spadl_conversion.R
\name{get_or_build_spadl}
\alias{get_or_build_spadl}
\title{Get or Build SPADL (with disk cache)}
\usage{
get_or_build_spadl(
  events,
  league,
  season,
  cache_dir = SPADL_CACHE_DIR,
  force_rebuild = FALSE
)
}
\arguments{
\item{events}{Raw Opta events from \code{load_opta_match_events()}.}

\item{league}{League code (e.g. "ENG"). Used only to build the cache key.}

\item{season}{Season string (e.g. "2024-2025"). Used only to build the cache key.}

\item{cache_dir}{Directory to read/write the cached \code{.rds}. Defaults to
\code{SPADL_CACHE_DIR} so all pipelines share one cache.}

\item{force_rebuild}{If \code{TRUE}, rebuild and overwrite the cache.}
}
\value{
Data frame in SPADL format, identical to \code{convert_opta_to_spadl()}.
}
\description{
Thin wrapper over \code{convert_opta_to_spadl()} that caches the result
on disk, keyed by \code{league} and \code{season}. SPADL conversion is the
single most expensive step in downstream EPV pipelines (~25\% of total
runtime per league-season) and is fully deterministic given raw events, so
caching yields a large win on repeat runs and backfills.
}
\seealso{
Other epv: 
\code{\link{aggregate_player_xmetrics}()},
\code{\link{assign_epv_credit}()},
\code{\link{convert_opta_to_spadl}()},
\code{\link{enrich_match_stats_with_xmetrics}()},
\code{\link{fit_epv_model}()},
\code{\link{fit_xg_model}()},
\code{\link{fit_xgot_model}()},
\code{\link{fit_xpass_model}()},
\code{\link{load_epv_model}()},
\code{\link{load_xg_model}()},
\code{\link{load_xgot_model}()},
\code{\link{load_xpass_model}()},
\code{\link{pb_download_epv_models}()},
\code{\link{predict_xg}()},
\code{\link{predict_xgot}()},
\code{\link{predict_xpass}()},
\code{\link{save_epv_model}()},
\code{\link{save_xg_model}()},
\code{\link{save_xpass_model}()}
}
\concept{epv}
