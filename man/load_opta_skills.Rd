% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opta_loaders.R
\name{load_opta_skills}
\alias{load_opta_skills}
\title{Load pre-computed Opta skill estimates}
\usage{
load_opta_skills(
  season = NULL,
  columns = NULL,
  source = c("remote", "local"),
  repo = "peteowen1/pannadata",
  tag = "opta-latest"
)
}
\arguments{
\item{season}{Optional season filter as end year (e.g., \code{2025} for
2024-2025 season).}

\item{columns}{Optional character vector of columns to select.}

\item{source}{Data source: \code{"remote"} (default, downloads from GitHub)
or \code{"local"} (reads from \code{opta_data_dir()}).}

\item{repo}{GitHub repository (default: "peteowen1/pannadata").}

\item{tag}{Release tag (default: "opta-latest").}
}
\value{
Data frame with one row per player-season containing skill estimates,
player metadata (\code{player_id}, \code{player_name},
\code{primary_position}), and context columns (\code{season_end_year},
\code{weighted_90s}, \code{total_minutes}).
}
\description{
Downloads and queries \code{opta_skills.parquet} from the \code{opta-latest}
GitHub release. This file contains Bayesian decay-weighted skill estimates
produced by the estimated skills pipeline (~15K player-seasons, ~2-3 MB).
}
\examples{
\dontrun{
# Load all skills
skills <- load_opta_skills()

# Load specific season
skills_2025 <- load_opta_skills(season = 2025)

# Use with player_skill_profile
player_skill_profile("H. Kane", skills = skills)
}
}
