% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cup_pairwise_model.R
\name{build_cup_pairwise_lookup}
\alias{build_cup_pairwise_lookup}
\title{Build a pooled-only, two-legged pairwise knockout lookup for one UEFA cup}
\usage{
build_cup_pairwise_lookup(
  match_dataset,
  goals_models,
  outcome_result,
  league,
  season,
  as_of = Sys.Date(),
  verbose = TRUE
)
}
\arguments{
\item{match_dataset}{The step-04 match dataset.}

\item{goals_models}{Step-05 goals models (\verb{$feature_cols} top-level,
\verb{$pooled$home} / \verb{$pooled$away}).}

\item{outcome_result}{Step-06 outcome models (\verb{$augmented_features}
top-level, \verb{$pooled$model}).}

\item{league}{One of \code{"UCL"}, \code{"UEL"}, \code{"UECL"}.}

\item{season}{Season label matching \code{match_dataset$season} for \code{league}.}

\item{as_of}{Reference date for "current state" (default: today). Exposed
for tests and reproducible snapshots.}

\item{verbose}{Print progress.}
}
\value{
A list:
\describe{
\item{probs}{data.table, one row per unordered team pair, with
\code{leg1_home_goals}/\code{leg1_away_goals}/\code{leg1_pH}/\code{leg1_pD}/\code{leg1_pA}
(t1 hosts) and the \verb{leg2_*} mirror (t2 hosts).}
\item{team_as_of}{named list, team -> Date of the row its features
were read from (freshness diagnostic).}
\item{n_teams}{league-phase team count (36 for the current UEFA
format).}
}
}
\description{
Predicts every possible league-phase-participant matchup with the full
goals + outcome models, two legs each (both hosting orientations, kept
separate -- see file header point 3). Companion to
\code{\link[=build_knockout_lookup]{build_knockout_lookup()}} for club cup competitions.
}
\concept{cup simulation}
