# Defense sign constraints – verbatim copy of 05_spm.R's `defense_good_features`/`defense_bad_features` (Section 10). Duplicated here (not sourced from 05_spm.R, which is a pipeline script, not a package function) so this file has no dependency on script execution order – mirrors the existing `.skill_spm_defense_constraints()` precedent (R/spm_opta.R) of a dedicated copy for a different column set.

Defense sign constraints – verbatim copy of 05_spm.R's
`defense_good_features`/`defense_bad_features` (Section 10). Duplicated
here (not sourced from 05_spm.R, which is a pipeline script, not a
package function) so this file has no dependency on script execution
order – mirrors the existing
[`.skill_spm_defense_constraints()`](https://peteowen1.github.io/panna/reference/dot-skill_spm_defense_constraints.md)
precedent (R/spm_opta.R) of a dedicated copy for a different column set.

## Usage

``` r
.panel_defense_sign_constraints()
```
