# Extract Tournament Host(s) From a Season String

Extract Tournament Host(s) From a Season String

## Usage

``` r
extract_tournament_hosts(season)
```

## Arguments

- season:

  Single season string.

## Value

Character vector of host team names (Opta-canonical). Empty character()
if season has no host concept (domestic / friendlies).
