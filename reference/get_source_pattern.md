# Get parquet file pattern for data source

Returns regex pattern to match parquet files for a source.

## Usage

``` r
get_source_pattern(source_type)
```

## Arguments

- source_type:

  One of "fbref", "understat", or "all"

## Value

Character regex pattern
