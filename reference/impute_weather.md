# Impute Missing Weather Values

Applies median imputation for missing continuous weather values and adds
log-transformed features.

## Usage

``` r
impute_weather(df)
```

## Arguments

- df:

  Data frame with weather columns

## Value

Data frame with imputed values and log transforms
