# Clear remote Opta data cache

Removes cached remote Opta data, forcing a fresh download on next
access.

## Usage

``` r
clear_remote_opta_cache()
```

## Examples

``` r
if (FALSE) { # \dontrun{
clear_remote_opta_cache()
} # }
```
