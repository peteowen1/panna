# Minimum position scale for successful passes

At x=0 (own goal), successful pass credit is scaled by this factor.
Ramps linearly to 1.0 at EPV_POSITION_RAMP_X.

## Usage

``` r
EPV_POSITION_SCALE_MIN
```

## Format

Numeric value: 0.3
