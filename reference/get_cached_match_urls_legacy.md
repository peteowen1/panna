# Get already-scraped match URLs from legacy cache

Get already-scraped match URLs from legacy cache

## Usage

``` r
get_cached_match_urls_legacy(data_type)
```

## Arguments

- data_type:

  Type of data to check

## Value

Character vector of match URLs already in cache
