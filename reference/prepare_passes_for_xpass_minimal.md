# Prepare Minimal Pass Features

Creates minimal features for xPass prediction from SPADL data. Used
internally when features aren't already prepared. Optimized with
data.table for fast processing.

## Usage

``` r
prepare_passes_for_xpass_minimal(passes)
```

## Arguments

- passes:

  SPADL pass actions

## Value

Data frame with pass features
