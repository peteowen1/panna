# Build duel contest features (internal helper)

Build duel contest features (internal helper)

## Usage

``` r
.create_duel_features(dt)
```

## Arguments

- dt:

  data.table with `x`, `y`.

## Value

`dt` with the four context feature columns added.
