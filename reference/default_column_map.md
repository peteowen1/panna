# Default column mapping for FBref/Opta data

Standard column name variations encountered across different data
sources.

## Usage

``` r
default_column_map()
```

## Value

Named list of canonical column names to alternatives

## Examples

``` r
if (FALSE) { # \dontrun{
col_map <- default_column_map()
names(col_map)
} # }
```
