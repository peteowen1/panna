# Create Location-Only Features

Creates a minimal feature set based only on location.

## Usage

``` r
create_location_features(x, y)
```

## Arguments

- x:

  X coordinates

- y:

  Y coordinates

## Value

Data frame with location features
