# Previous manifest or NULL on first publish (transient errors propagate)

Previous manifest or NULL on first publish (transient errors propagate)

## Usage

``` r
vb_read_prev_manifest(repo, tag)
```
