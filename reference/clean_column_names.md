# Clean column names to snake_case

Applies janitor::clean_names() to standardize all column names.

## Usage

``` r
clean_column_names(data)
```

## Arguments

- data:

  Data frame

## Value

Data frame with snake_case column names
