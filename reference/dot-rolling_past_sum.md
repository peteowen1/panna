# Compute strictly-past rolling-sum within a window for one ordered series

Compute strictly-past rolling-sum within a window for one ordered series

## Usage

``` r
.rolling_past_sum(value, date_int, window_days)
```
