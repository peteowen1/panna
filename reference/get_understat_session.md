# Get or create Understat session

Returns a persistent httr session handle that maintains cookies.

## Usage

``` r
get_understat_session(reset = FALSE)
```

## Arguments

- reset:

  If TRUE, creates a new session (default FALSE)

## Value

httr handle object
