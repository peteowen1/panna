# Process match lineups

Cleans lineup data and extracts player information. Uses data.table for
speed with large datasets.

## Usage

``` r
process_match_lineups(lineups, results)
```

## Arguments

- lineups:

  Data frame of match lineups

- results:

  Processed match results for match IDs

## Value

Cleaned data frame with player lineup info
